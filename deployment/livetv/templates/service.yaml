apiVersion: v1
kind: Service
metadata:
  name: {{ include "livetv.fullname" . }}
  labels:
    {{- include "livetv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "livetv.selectorLabels" . | nindent 4 }}
